# bv (beads viewer) local config and caches
.bv/

# Reference libraries cloned for development (synced via scripts/pull_latest_reference_library_repos.sh)
/legacy_reference_library_code/

# Rust build artifacts
/target/
Cargo.lock
fuzz/target/
fuzz/artifacts/

# Proptest regression files (generated)
**/proptest-regressions/
fuzz/corpus/

# Build artifacts
a.out

# Profiling data (llvm-profdata)
*.profraw

# perf profiling data
perf.data
perf.data.*
perf_*.data

# NFS temporary files
.nfs*

# macOS resource fork files
._*

# Flamegraph output
flamegraph*.svg

# Test checksums (ephemeral)
golden_checksums.txt

# Local scratch DBs
storage.sqlite3
